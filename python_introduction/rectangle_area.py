# length with a value representing the length of the rectangle.
# width with a value representing the width of the rectangle.
# Calculate the area of the rectangle using the formula (Area = length × width)
# Print the area of the rectangle in a formatted string.# Rectangle Area Calculation
# Rectangle Area Calculation

length = 10  # Length of the rectangle
width = 5   # Width of the rectangle

# Calculate the area of the rectangle
area = length * width
# Print the area of the rectangle

print("The Area of the rectangle with length " + str(length) + " and width " + str(width) + " is " + str(area))
# The Area of the rectangle with length 10 and width 5 is 50